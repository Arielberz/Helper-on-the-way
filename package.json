{
  "name": "helper-on-the-way",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time roadside assistance web app",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "build:client": "npm run build --prefix client",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "start:server": "npm run start --prefix server",
    "verify": "npm run install:all && npm run build:client && node scripts/verify.mjs"
  },
  "keywords": [
    "roadside-assistance",
    "real-time",
    "socket.io",
    "react",
    "express"
  ],
  "author": "",
  "license": "ISC"
}
